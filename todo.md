# SkillVerse UI Redesign — SELECT MATCH Screen

## Plan
Build a new "SELECT MATCH" matchmaking interface matching the reference image, integrate into the SkillVerse webapp, verify it renders correctly, and push to the repo as a PR.

## Tasks
- [x] Clone & inspect the skillverse repo
- [x] Analyze reference design (SELECT MATCH cyberpunk neon UI)
- [ ] Create new CSS for the SELECT MATCH screen
- [ ] Build the SELECT MATCH screen HTML (header, 3 match cards, why-play section, bottom nav)
- [ ] Wire up JS interactions (mode selection, find match)
- [ ] Integrate into index.html flow (dashboard -> select match)
- [ ] Preview/verify rendering in browser
- [ ] Commit to a new branch & push to GitHub
- [ ] Open a Pull Request
